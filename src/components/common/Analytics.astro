---
import { contacts } from '@/data/contacts';

const metrikaId = contacts.analytics.yandexMetrikaId;
---

<script is:inline define:vars={{ metrikaId }}>
  // Счётчик подключается сразу: посетителя предупреждает уведомление внизу
  // страницы («продолжая пользоваться сайтом, вы соглашаетесь…»).
  if (metrikaId) {
    (function (m, e, t, r, i, k, a) {
      m[i] =
        m[i] ||
        function () {
          (m[i].a = m[i].a || []).push(arguments);
        };
      m[i].l = 1 * new Date();
      for (let j = 0; j < e.scripts.length; j++) {
        if (e.scripts[j].src === r) return;
      }
      k = e.createElement(t);
      a = e.getElementsByTagName(t)[0];
      k.async = 1;
      k.src = r;
      a.parentNode.insertBefore(k, a);
    })(window, document, 'script', 'https://mc.yandex.ru/metrika/tag.js?id=' + metrikaId, 'ym');

    window.ym(metrikaId, 'init', {
      ssr: true,
      webvisor: true,
      clickmap: true,
      ecommerce: 'dataLayer',
      referrer: document.referrer,
      url: location.href,
      accurateTrackBounce: true,
      trackLinks: true,
    });
  }

  window.cleanPlanetTrack = function (eventName, payload = {}) {
    if (window.ym && metrikaId) window.ym(metrikaId, 'reachGoal', eventName, payload);
  };

  const utmKeys = ['utm_source', 'utm_medium', 'utm_campaign', 'utm_content', 'utm_term'];
  const url = new URL(window.location.href);
  const storedUtm = {};
  utmKeys.forEach((key) => {
    const value = url.searchParams.get(key);
    if (value) storedUtm[key] = value;
  });
  if (Object.keys(storedUtm).length)
    sessionStorage.setItem('cleanplanet_utm', JSON.stringify(storedUtm));

  document.addEventListener('click', (event) => {
    const target = event.target.closest('[data-track]');
    if (target) window.cleanPlanetTrack(target.dataset.track);
  });
</script>

<noscript>
  <div>
    <img
      src={`https://mc.yandex.ru/watch/${metrikaId}`}
      style="position:absolute; left:-9999px;"
      alt=""
    />
  </div>
</noscript>
